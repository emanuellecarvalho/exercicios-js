/* const a = [2, 3, 1, 0, 5];
const b = [1, 2, 3, 4, 5];

const tem = [];
const all = b;
a.map((item) => {
    if(b.indexOf(item) != -1){
        tem.push(item);
    } else {
        all.push(item);
    }
});

console.log(tem);
console.log(all); */

function ArrayChallenge(strArr) {
    const arrayOne = [1, 2, 3, 4, 5];
    const arrayTwo = [2, 3];

    const intersection = arra.filter(i => a2.includes(i));

}




console.log(intersection);
